architectury {
    common(rootProject.enabled_platforms.split(","))
}

loom {
    accessWidenerPath = file("src/main/resources/stonezone.accesswidener")
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    modApi "net.fabricmc.fabric-api:fabric-api:${rootProject.fabric_api_version}"


    annotationProcessor 'net.fabricmc:sponge-mixin:0.12.5+mixin.0.8.5'

//!! MOONLIGHT LIB (REQUIRED)  -------------------------------------------------------------------------------------- \\

//    def path = System.getenv('REPOS20_1') + "\\Moonlight\\fabric\\build\\libs\\moonlight-${moonlight_version}-fabric.jar"
//    modImplementation(files(path)) // Importing compiled mod from Moonlight's Repository

//    modImplementation("net.mehvahdjukaar:moonlight:${moonlight_version}-fabric") // LOCAL
    modImplementation("curse.maven:selene-499980:5973714") // v2.13.36

//!! EVERY COMPAT (REQUIRED)
    modImplementation("curse.maven:every-compat-628539:5967268") // v2.7.1

//!! ================================================= DEPENDENCIES ================================================= \\


//!! =================================================== IMPORTS ==================================================== \\

    //!! The module really don't need library from the mod, so it's fine to ignore
//    modCompileOnly("curse.maven:create-fabric-624165:5958511")

    //!! REQUIRED - The module need libaries from the mod
    modCompileOnly("curse.maven:twigs-496913:4603805")
    modCompileOnly("curse.maven:building-but-better-989479:5382599")
    modCompileOnly("curse.maven:stoneworks-852663:4611947")

}

publishing {
    publications {
        mavenCommon(MavenPublication) {
            artifactId = rootProject.mod_id
            from components.java
        }
    }

    repositories {
        // Add repositories to publish to here.
    }
}
